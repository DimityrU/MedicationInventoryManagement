@model User;

@{
    ViewData["Title"] = "Log In";
}

<h1 class="text-center mb-5">Medication Inventory Management</h1>

<div class="container d-flex align-items-center justify-content-center">
    <div class="card mx-auto login-form">
        <div class="card-body">
            <h2 class="card-title text-center">Log In</h2> 
            <form asp-action="LogIn">
                <div class="form-group">
                    <label asp-for="UserName" class="control-label"></label>
                    <input asp-for="UserName" placeholder="Enter your username" class="form-control"/>
                </div>
                <div class="form-group">
                    <label asp-for="Password" class="control-label"></label>
                    <input asp-for="Password" placeholder="Enter your password" class="form-control" type="password"/>
                </div>
                @if (!ViewData.ModelState.IsValid)
                {
                    <div class="alert alert-danger mt-2 p-2 text-center" role="alert">
                        @foreach (var error in ViewData.ModelState.Values.SelectMany(v => v.Errors))
                        {
                            <p class="m-0">@error.ErrorMessage</p>
                        }
                    </div>
                }
                <div class="form-group">
                    <input type="submit" class="btn btn-primary mt-2" value="Log In"/>
                </div>
            </form>
        </div>
    </div>
</div>
